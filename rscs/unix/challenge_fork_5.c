#include <stdio.h>
#include <unistd.h>
#include <signal.h>
#include <sys/wait.h>
#include <stdlib.h>
/*
    Escreva um programa que cria dois processos filhos usando a função fork(). O primeiro filho deve executar o comando "ls -l" e exibir o resultado na tela, enquanto o segundo filho deve executar o comando "ps -ef" e exibir o resultado na tela.
*/
int main(void)
{
    return (0);
}